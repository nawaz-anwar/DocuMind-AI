{
  "version": 2,
  "buildCommand": "pnpm build:backend && rm -rf dist && cp -R apps/backend/dist dist",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": null,
  "outputDirectory": "dist"
}
